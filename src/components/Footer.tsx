// This component is no longer used.
// Footer is now integrated into ContactSection.
// Kept as an empty export to avoid import errors if referenced elsewhere.

const Footer = () => null;
export default Footer;
